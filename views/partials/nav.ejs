<header style="border-bottom: 1px solid var(--border); margin-bottom: 32px;">
  <div class="container-wide" style="display: flex; justify-content: space-between; align-items: center; height: 56px;">
    <a href="/" style="font-weight: 800; font-size: 1.05rem; color: var(--text-primary); letter-spacing: -0.02em;">
      IG Dashboard
    </a>
    <nav class="flex gap-sm">
      <a href="/" class="btn btn-ghost btn-sm">홈</a>
      <a href="/accounts" class="btn btn-ghost btn-sm">계정</a>
      <form method="POST" action="/logout" style="margin:0">
        <button type="submit" class="btn btn-ghost btn-sm" style="color: var(--text-tertiary);">로그아웃</button>
      </form>
    </nav>
  </div>
</header>
